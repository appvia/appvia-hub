<h1>Users</h1>

<% @users.each do |user| %>
  <div class="card shadow-sm mb-3">
    <div class="card-body">
      <h5 class="card-title">
        <% if user.admin? %>
          <span class="badge badge-success float-right">
            Hub admin
          </span>
        <% end %>

        <%= user.email %>
      </h5>
      <% if user.name.present? %>
        <h6 class="card-subtitle mb-2 text-muted">
          <%= user.name %>
        </h6>
      <% end %>
    </div>
    <% if current_user.admin? %>
      <div class="card-footer text-muted">
        <% if user.admin? %>
          <%= update_user_role_link 'Revoke hub admin', user, :user %>
        <% else %>
          <%= update_user_role_link 'Make hub admin', user, :admin %>
        <% end %>
      </div>
    <% end %>
  </div>
<% end %>
